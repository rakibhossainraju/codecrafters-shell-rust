/// Execute external (non-builtin) commands
/// This is a placeholder for now - later will use std::process::Command
pub fn execute_external(name: &str, _args: &[String]) {
    println!("{}: command not found", name);
}

